from django.apps import AppConfig

class ModoDesparcheConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name         = 'modules.modo_desparche'
    label        = 'kora_desparche'
    verbose_name = 'Modo Desparche — Juegos'
